<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="30dp"
    android:paddingTop="30dp">

    <TextView
        android:id="@+id/lbl_userId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="userId"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/lbl_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="id"
        app:layout_constraintStart_toStartOf="@+id/lbl_userId"
        app:layout_constraintTop_toBottomOf="@+id/lbl_userId" />

    <TextView
        android:id="@+id/lbl_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        tools:text="Title"
        app:layout_constraintStart_toStartOf="@+id/lbl_id"
        app:layout_constraintTop_toBottomOf="@+id/lbl_id" />

    <TextView
        android:id="@+id/lbl_body"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Body"
        app:layout_constraintStart_toStartOf="@+id/lbl_title"
        app:layout_constraintTop_toBottomOf="@+id/lbl_title" />
</androidx.constraintlayout.widget.ConstraintLayout>